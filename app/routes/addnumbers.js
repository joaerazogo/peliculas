import Route from '@ember/routing/route';

export default Route.extend({
  model(){
    var minNumber;
    var maxNumber;
    var sum;
    var message;

    return{
      sum,
      message,
    };
  }
});
